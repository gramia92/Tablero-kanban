# Copiar este archivo a .env (cp .env.example .env) antes de correr docker compose up.
# Los valores ya vienen llenos por defecto para desarrollo/evaluacion local — no son secretos
# de produccion, son solo para que la app funcione de una sin pasos manuales extra.

POSTGRES_DB=kanban
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

JWT_SECRET=l+NauKm4As2n3tU3ecJNnQ17vy+QMu8bi/7C+KMG439XNbi2Xb+wJQvkfjuo4v6i
JWT_ISSUER=IdeasGroup.Kanban.Api
JWT_AUDIENCE=IdeasGroup.Kanban.Client
JWT_EXPIRY_MINUTES=60
